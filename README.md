# quahog-airlift

requires 'secrets.py' file:

```
secrets = {
    'ssid' : b'wifi_essid',
    'password' : b'wifi_password',
    'farmos_pubkey' : 'public_key',
    'farmos_privkey' : 'private_key'
    }
```
